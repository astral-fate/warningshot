# Datasheet — labelled attack corpus

Generated by `scripts/prepare_corpus.py`. Do not hand-edit.

## Why this corpus exists

To answer one question: **does a detector built for a single attack class stay
quiet on other attack classes and on benign traffic?** That is a specificity
question. The data available can answer it. It cannot answer the matching
sensitivity question for transport-level covert channels, and this datasheet
says so in the same breath rather than in a footnote.

## Provenance

| Source | Ships | Role |
|---|---|---|
| `agent-egress-bench` | 256 cases, 18 families, every case labelled | **primary** |
| `sandbox_escape_bench` | 18 scenario definitions with exploit code | taxonomy only |
| `sandbox_escape_bench` transcripts | **nothing** — 162 indexed runs, 0 resolvable | **unusable** |

Counts were cross-checked against agent-egress-bench's own `cases/STATS.md`
and agree exactly (256 cases, 18 families,
block 186 / allow 69 /
warn 1). `tests/test_corpus.py::test_matches_upstream_stats`
enforces that agreement, because an earlier loader walked `*.json` only and
silently reported 250 cases and 17 families, dropping the corpus's only
`warn`-labelled case.

### The transcript gap, stated plainly

`sandbox_escape_bench/transcript-analysis/.../included_logs.csv` indexes
162 eval runs across 19 scenarios and 9 models. Its `log`
column holds absolute paths of the form:

    /Users/jeromewynne/Documents/docker-breakout-scans/data/final/smaller-models-anthropic-openai/2026-01-26T18-32-15+00-00_bpf-privesc_BAKo6KddGde4qvHy3WMdBN.eval

None resolve inside the clone. **Per-sample transcripts are not recoverable**,
so this corpus supports no per-episode detection scoring. Aggregate
behaviour-scan tables under `transcript-analysis/results/` remain readable as
priors. This is the third dataset in this project that indexes data it does not
ship; the first was the Hugging Face 17,600-action replay (HTTP 403).

## Ground truth

`expected_verdict` in `block` / `allow` / `warn`, authored by the corpus
maintainers, not by us. Self-labelling circularity is a named reviewer
complaint in this programme, and this corpus avoids it: we did not write these
labels and cannot tune them.

- attack (`block`): **186**
- benign (`allow`/`warn`): **70**
- purpose-built false-positive traps (`false_positive` family): **22**
- benign controls inside attack families: **48**
- benign cases upstream labels `high` false-positive risk: **24**

### By family

| family | n | block | allow | warn |
|---|---:|---:|---:|---:|
| `mcp_tool` | 32 | 23 | 9 | 0 |
| `request_body` | 25 | 18 | 7 | 0 |
| `url` | 23 | 20 | 3 | 0 |
| `false_positive` | 22 | 0 | 22 | 0 |
| `mcp_chain` | 17 | 13 | 4 | 0 |
| `mcp_input` | 15 | 12 | 3 | 0 |
| `a2a_message` | 14 | 11 | 3 | 0 |
| `headers` | 14 | 10 | 4 | 0 |
| `crypto_financial` | 11 | 10 | 1 | 0 |
| `ssrf_bypass` | 11 | 10 | 1 | 0 |
| `websocket_dlp` | 11 | 9 | 2 | 0 |
| `hostname_exfiltration` | 10 | 9 | 1 | 0 |
| `shell_obfuscation` | 10 | 8 | 2 | 0 |
| `encoding_evasion` | 9 | 9 | 0 | 0 |
| `response_fetch` | 9 | 6 | 3 | 0 |
| `response_mitm` | 9 | 8 | 1 | 0 |
| `a2a_agent_card` | 8 | 6 | 2 | 0 |
| `mcp_drift` | 6 | 4 | 1 | 1 |

## The temporal sub-corpus

Six `mcp_drift` cases pair a baseline snapshot with a later one. Their own
threat model calls tool metadata a covert channel, so the broad reading of the
upstream README's covert-channel exclusion is wrong — that exclusion is about
transport-level channels. These six are the closest shipping analogue to a
channel that reappears after a substrate is wiped, because the discrimination
they demand is *malicious mutation* versus *legitimate change*.

| case | label | before (chars) | after (chars) |
|---|---|---:|---:|
| `mcp-drift-benign-001` | `warn` | 462 | 682 |
| `mcp-drift-collusion-004` | `block` | 785 | 997 |
| `mcp-drift-http-benign-refinement-006` | `allow` | 168 | 214 |
| `mcp-drift-http-rugpull-desc-005` | `block` | 206 | 372 |
| `mcp-drift-rugpull-desc-002` | `block` | 277 | 437 |
| `mcp-drift-rugpull-param-003` | `block` | 282 | 411 |

A detector reading only a flattened payload cannot solve these; it has to
compare two states. That is why `before_text` and `after_text` are kept apart
in the record rather than concatenated.

## What this corpus can establish

- specificity: how often a detector fires on non-covert attack traffic
- false-positive rate against 22 purpose-built over-blocking traps
- the harder false-positive test: benign controls inside attack families, which resemble the attack they sit beside
- per-family firing rates across 18 independent attack families

## What it cannot establish

- sensitivity to covert channels: absent from this corpus by design (agent-egress-bench README: 'Covert channels. Timing, header ordering, HTTP/2 framing, and steganography are out of scope by design.')
- detection on agent transcripts: these cases are wire payloads, not multi-step agent episodes

## Files

| file | contents |
|---|---|
| `egress_cases.jsonl` | all 256 labelled cases |
| `temporal_cases.jsonl` | the 6 before/after cases |
| `escape_scenarios.json` | 18 breakout scenario definitions |
| `coverage.json` | this datasheet's numbers, machine-readable |
